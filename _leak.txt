שינוי בכמות בלבד.</li>'
     + '<li>הנתח נמדד בנקודות אחוז, לא באחוזים. אלה שני מדדים שונים.</li></ul></div>';
  openSheet(p.period_label + (p.is_partial ? ' (חלקי)' : ''), b);
}

// הגוש קיים רק אצל דורון. אצל כל השאר הוא נמחק בשרת, וכל מה שתלוי בו
// פשוט לא קיים — לא מוסתר, לא קיים.


/* ---------- chips ---------- 